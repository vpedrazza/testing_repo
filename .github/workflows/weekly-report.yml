name: Weekly Report
on:
  push:
    branches: [ main ]
#
# schedule:
#   - cron: '0 18 * * 5' # At 06:00 on Sunday
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: wow-actions/activity-report@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
